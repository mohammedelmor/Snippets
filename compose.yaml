services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=code_snippet'
      - 'POSTGRES_PASSWORD=password'
      - 'POSTGRES_USER=user'
    ports:
      - '5432:5432'
